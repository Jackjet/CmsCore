@model CmsCore.Areas.Manage.Models.CategoryCreateParam
@{
    ViewBag.Title = "添加栏目";
}
<div class="row">

    <div class="col-md-3">

        @Html.Partial("Shared/_QuickMenu")

    </div>

    <div class="col-md-9 ">

        <ol class="breadcrumb">
            <li><a href="/Manage">管理后台</a></li>
            <li><a href="/Manage/Category">栏目管理</a></li>
            <li class="active">添加栏目</li>
        </ol>

        <div class="container">

            @using (Html.BeginForm("create", "Category", FormMethod.Post, new { @class = "form-horizontal" }))
            {
                @Html.ValidationSummary(true)

                <div class="form-group">
                    @Html.LabelFor(model => model.MetaTitle, new { @class = "col-md-2 control-label" })
                    <div class="col-md-10">
                        @Html.TextBoxFor(model => model.MetaTitle, new { @class = "form-control", style = "width: 280px" })
                        @Html.ValidationMessageFor(model => model.MetaTitle)
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <button type="submit" class="btn btn-primary">创建</button>
                        <a href="/manage/category" class="btn btn-default">取消</a>
                    </div>
                </div>
            }

        </div>

    </div>

</div>